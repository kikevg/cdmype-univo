<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Login
        //- bootstrap
        link(rel="stylesheet", href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css")
        //- google fonts
        link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;900&display=swap")
        //- font awesome
        script(src="https://kit.fontawesome.com/2b5123bf0f.js")
        block links
        //- custom css
        link(rel="stylesheet", href="/public/css/style.css")
    body
        div(style="height: 100vh;" class="d-flex align-items-center justify-content-center")
            //- login
            div(style="width: 300px")
                img(src="/public/img/cdmype.jpg", alt="" width="300" class="img-fluid my-5")
                if message != null
                    .alert.alert-danger.alert-dismissible.fade.show= message
                        button(type="button" class="close" data-dismiss="alert" aria-label="Close")
                            span(aria-hidden="true") &times;
                form(action="/admin", method="post")
                    .form-group
                        input(type="text", name="username" class="form-control bg-light border-0 py-4" placeholder="Nombre de usuario")
                    .form-group
                        input(type="password", name="password" class="form-control bg-light border-0 py-4" placeholder="Contraseña")
                    .form-group
                        button(type="submit" class="btn btn-block btn-primary py-4") Iniciar sesion
    //- jquery
    script(src="https://code.jquery.com/jquery-3.5.1.min.js")
    //- bootstrap
    script(src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js")